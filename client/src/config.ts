import env from "react-dotenv";

export const {
    API_URL,
    AUTH_URL
} = env

console.log('sqweqw', API_URL, AUTH_URL)

export const {
    UI_PORT,
    UI_HOST
} = env

console.log('sqweqw', API_URL, AUTH_URL)